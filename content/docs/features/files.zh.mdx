---
title: 下载文件
description: HTTP 和 FTP 文件下载。
---

## HTTP

粘贴链接或从浏览器拦截。GD 自动多线程下载，速度不够时自动增加线程。

支持断点续传：暂停后可继续，不从头开始。

<Callout type="info">
线程数可在新建任务时调整，也可以在下载中 **右键 → 编辑** 修改。
</Callout>

---

## FTP

粘贴 `ftp://` 或 `ftps://` 链接。如果链接指向目录，会列出文件树供勾选。

和 HTTP 一样支持多线程和断点续传。代理仅支持 SOCKS4/SOCKS5。
