---
title: Download Categories
description: Auto-sort downloads by file type into folders.
---

**Settings → Download Categories** → enable.

Each category has a name, icon, extension list (e.g. `mp4, mkv`), and optional download folder.

Use `{default}/Videos` to put categorized files in a subfolder of the default download directory.
